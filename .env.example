# Copy to .env in this folder. Used by api_server.py and the verification services.

# ── LLM verification (OpenAI Chat Completions) ──
OPENAI_API_KEY=your_openai_key
# Optional: defaults to gpt-4o-mini
OPENAI_CHAT_MODEL=gpt-4o-mini

# ── Apify Social Media Finder actor ──
APIFY_TOKEN=your_apify_token
# Actor id or "user~actor-name" slug of the social-media-finder actor
APIFY_ACTOR_ID=your_actor_id
# Optional JSON input template. May use {name} and {website} placeholders.
# Defaults to '{"name": "{name}"}' when unset.
APIFY_ACTOR_INPUT={"name": "{name}"}
# Optional sync-run timeout in seconds (default 120)
APIFY_TIMEOUT_SECONDS=120

# ── Serper.dev (fallback search for platforms Apify/Wikidata do not cover) ──
SERPER_API_KEY=your_serper_key

# ── Frontend CORS (comma-separated origins for production) ──
# Example: https://your-app.vercel.app,https://yourdomain.com
CORS_ORIGINS=http://localhost:3000

# ── Postgres for analyst decisions (verified_url / rejected_url) ──
# Run sql/001_create_url_tables.sql in pgAdmin first, then set this.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/YOUR_DB

# ── Auth & limits (require DATABASE_URL; run sql/003 + create_user.py) ──
AUTH_REQUIRED=1            # set 0 to run the API open (local dev only)
SESSION_TTL_HOURS=12
MAX_ROWS_PER_JOB=5000      # hard ceiling per run — protects the API budget

# ── Google sign-in (optional; leave blank for password-only login) ──
# "Web application" OAuth client from Google Cloud Console → Credentials.
# Add your frontend origin (e.g. http://localhost:3000) to that client's
# "Authorised JavaScript origins", or the browser button will not appear.
# The client ID is public — the backend verifies every ID token against
# Google's keys, so it is safe for the UI to read it from /api/auth/status.
GOOGLE_CLIENT_ID=70751413093-o7esvnm63l440af545ivg3onjgseopjt.apps.googleusercontent.com
# Comma-separated domains allowed to sign in and be created on first use,
# e.g. listenfirstmedia.com. Leave EMPTY to allow only pre-existing accounts:
# without it, an OAuth client alone would let any Google account in the world in.
GOOGLE_ALLOWED_DOMAINS=@listenfirstmedia.com
