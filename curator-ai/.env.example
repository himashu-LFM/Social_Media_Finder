# Copy to .env.local and adjust. Python API (FastAPI) runs separately — see C:\Testing\api_server.py

# Base URL of the Python backend (no trailing slash). If unset, Processing uses client-side demo only.
NEXT_PUBLIC_PYTHON_API_URL=http://127.0.0.1:8787
